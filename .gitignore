# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Logs (the launchd plist writes to ~/Desktop/shared/logs, but be safe)
*.log

# Local-only photo backup the optimization agent may leave behind
assets/img/products-original-backup/

# Node / npm (we're framework-free today, but future-proofing)
node_modules/
.env
.env.local
.env.*.local

# OS junk
Thumbs.db
